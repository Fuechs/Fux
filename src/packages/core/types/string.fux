package types;

get cppfux;

cppfux->storage_type:_string_type {
    
    instruction ( self->name[->str][=self->value]; );

    split(self, str cut_point) -> str {

        // soon
        pass;
        
    }   

    length(self) -> int {

        characters[] -> char = (char)self->value->split << '';

        count -> int = 0;
        for (char character) in (characters[]) count++;

        return count;

    }

}