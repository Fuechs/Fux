modify(x -> i64): void;

main(final argc -> u64, final argv -> str[]): i64 {
    x: i64 = argc + 1;
    modify(x);
    return x;
}

intern *test; // FIXME: parser won't recognize the access modifier and parse the '*' as a dereference instead